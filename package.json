{
  "name": "portfolio-clone",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:photography": "node scripts/generate-photography-assets.mjs",
    "predev": "npm run generate:photography",
    "dev": "vite --host 127.0.0.1",
    "prebuild": "npm run generate:photography",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "5.0.4",
    "vite": "6.4.2",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {}
}
